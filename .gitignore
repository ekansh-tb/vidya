node_modules
.next
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
.DS_Store

# clerk configuration (can include secrets)
/.clerk/
# Any local env file. `vercel env pull` writes real secrets into these.
.env*
# ...except the committed template, which contains no values.
!.env.local.example
